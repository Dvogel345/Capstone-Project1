// Walls -> layer
// layer 1 -> walls
// layer -> width 0, width size -1 
// Collider 0 == Wall, left and right

// x == 0 = ground
// ground --> layer 2 
// layer 2 -> state -> isGrounded

// death = { player1 -> death
// if player == death, then
// player pos == to initial spawn
// or game level == to beginning/ start}

// Coins = x random, y random
// coin shape -> limits -> x(30) and y(30)